{
  "name": "@designgreat/app-service-serverless-a",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "dev": "pnpm exec node --watch src/index.ts",
    "build": "tsc -p ../../tsconfig.library.json",
    "typecheck": "tsc --noEmit -p ../../tsconfig.node.json",
    "lint": "pnpm exec eslint src --max-warnings 0",
    "test": "echo \"test not implemented yet\"",
    "clean": "rm -rf dist tsconfig*.tsbuildinfo"
  }
}
