{
  "name": "@designgreat/lib-web-ui-website",
  "version": "0.2.0",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "dev": "turbo run dev --filter=@designgreat/lib-web-ui-website",
    "build": "tsc -p ../../tsconfig.react.json",
    "typecheck": "tsc --noEmit -p ../../tsconfig.react.json",
    "lint": "pnpm exec eslint src --max-warnings 0",
    "test": "echo \"test not implemented yet\"",
    "clean": "rm -rf dist tsconfig*.tsbuildinfo"
  }
}
